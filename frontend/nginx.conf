server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    location = / {
        return 302 ${APP_BASE_PATH};
    }

    location = ${APP_BASE_PATH} {
        return 302 ${APP_BASE_PATH}/;
    }

    location ${APP_BASE_PATH}/api/product/ {
        rewrite ^${APP_BASE_PATH}/api/product/(.*)$ /api/product/$1 break;
        proxy_pass ${PIMCORE_API_BASE_URL};
        proxy_http_version 1.1;
        proxy_ssl_server_name on;
        proxy_set_header Authorization "Bearer ${PIMCORE_BEARER_TOKEN}";
        proxy_set_header Accept "application/json";
        proxy_set_header Host stage.pimcore.argip.advox.dev;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location ${APP_BASE_PATH}/api/ {
        rewrite ^${APP_BASE_PATH}/api/(.*)$ /$1 break;
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Prefix ${APP_BASE_PATH};
    }

    location ~* ^${APP_BASE_PATH}/.+\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        rewrite ^${APP_BASE_PATH}/(.*)$ /$1 break;
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location ${APP_BASE_PATH}/ {
        rewrite ^${APP_BASE_PATH}/(.*)$ /$1 break;
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-store, no-cache, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
    }

    location = /index.html {
        add_header Cache-Control "no-store, no-cache, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
    }

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}
